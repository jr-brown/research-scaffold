# Compositional meta-config for use as sweep base_config
# This produces multiple configs that will all run in each sweep iteration

folder: "configs/lennie"
common_root: "root.yaml"

experiments:
  # Two different configurations to sweep over
  - config: "level1a.yaml"
  - config: "level1b.yaml"

