# Meta-config running multiple sweeps sequentially

experiments:
  # First sweep: optimize learning rate
  - sweep_config: "sweeps/sweep_configs/basic_sweep.yaml"
    sweep_count: 5
  
  # Second sweep: explore batch sizes
  - sweep_config: "sweeps/sweep_configs/for_composition.yaml"
    base_config: "sweeps/configs/sweep_base.yaml"
    sweep_count: 4

